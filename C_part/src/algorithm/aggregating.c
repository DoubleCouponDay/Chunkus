#pragma once

#include "aggregating.h"

#include <stdlib.h>
#include <stdio.h>

#include "../chunkmap.h"

void OR_64(pixelchunk** compare1, pixelchunk** compare2, pixelchunk** output, int width, int height) {
    for(int x = 0; x < width; ++x) {
        for(int y = 0; y < height; ++y) {
            
        }
    }
}
